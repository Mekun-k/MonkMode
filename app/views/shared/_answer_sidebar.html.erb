<div class="listings">
  <div class="listings_channels ml-3">
    <h3 class="listings_header mb-3">振り返り</h4>
    <ul class="channel_list">
      <div class="mb-3">
        <%= sidebar_link_item('設定', rules_path) %>
      </div>
      <div class="mb-3">
        <%= sidebar_link_item('実行', new_answer_path) %>
      </div>
      <div class="mb-3">
        <% if params[:id].present? %>
          <%= sidebar_link_item('詳細', answer_path(params[:id])) %>
        <% end %>
      </div>
      <div class="mb-3">
        <%= sidebar_link_item('結果一覧', answers_path) %>
      </div>
    </ul>
  </div>
</div>
